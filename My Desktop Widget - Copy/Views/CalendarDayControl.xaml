<UserControl x:Class="MyCalendarWidget.Views.CalendarDayControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignHeight="150" d:DesignWidth="100">

    <Border x:Name="DayBorder" 
            BorderBrush="#22FFFFFF" 
            BorderThickness="0.5" 
            Margin="1" 
            Background="Transparent" 
            Cursor="Hand"
            MouseLeftButtonUp="DayBorder_MouseLeftButtonUp">
        <Grid IsHitTestVisible="False">
            <Grid.RowDefinitions>
                <RowDefinition Height="3*"/>
                <RowDefinition Height="1*"/>
            </Grid.RowDefinitions>

            <Viewbox Grid.Row="0" Stretch="Uniform" VerticalAlignment="Center" Margin="2,2,2,0">
                <StackPanel Width="150">
                    <TextBlock x:Name="txtDayNumber" 
                               Foreground="White" 
                               FontSize="25" 
                               FontWeight="ExtraBold" 
                               HorizontalAlignment="Center" 
                               TextAlignment="Center"/>

                    <TextBlock x:Name="txtLunarDay"
                               FontSize="16" 
                               FontWeight="ExtraBold" 
                               Foreground="#9900FFFF"      
                               HorizontalAlignment="Center"  
                               Margin="0,-8,0,0"/>
                </StackPanel>
            </Viewbox>

            <Viewbox Grid.Row="1" Stretch="Uniform" VerticalAlignment="Bottom" Margin="4,0,4,5">
                <Grid Width="150">
                    <TextBlock x:Name="txtEventCount" 
                               Foreground="Beige" 
                               FontSize="22" 
                               FontWeight="Bold"
                               Visibility="Collapsed" 
                               HorizontalAlignment="Center" 
                               TextAlignment="Center"/>

                    <ItemsControl x:Name="itemsEvents" Visibility="Collapsed"/>
                </Grid>
            </Viewbox>
        </Grid>
    </Border>
</UserControl>